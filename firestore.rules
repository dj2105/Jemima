// ⚠️ DEV-ONLY RULES — for local emulator testing.
// Everything readable/writable to eliminate rules as a variable.
// Do NOT deploy these to production.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
